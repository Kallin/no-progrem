def languages = ['java', 'groovy', 'scala', 'cplusplus']
def projects = ['language', 'patterns', 'problems']

projects.each { project ->
    include project
    languages.each { language ->
        include "$project:$language"
    }
}